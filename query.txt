SELECT cast(list(trim(COLUMN_NAME)) as varchar(2000)) AS COLUMN_NAME
from
(
SELECT R.RDB$FIELD_NAME AS COLUMN_NAME
    FROM RDB$RELATION_FIELDS R
    WHERE R.RDB$RELATION_NAME = 'ISI_NAMA_TABLE'
    ORDER BY R.RDB$FIELD_POSITION
)X